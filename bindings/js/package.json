{
	"name": "@idni/tau-lang-js-test-tooling",
	"private": true,
	"version": "0.0.0",
	"description": "Dev-only tooling for bindings/js/tests (the headless-Chrome wasm test harness). Not the published package -- that manifest is bindings/js/package.json.in, configure_file'd into the build directory, which *is* the installable package.",
	"devDependencies": {
		"puppeteer-core": "25.7.0"
	}
}
